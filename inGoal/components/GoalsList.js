import React from "react";
import { View, Text } from "react-native";

const GoalsList = ({ value }) => {
  return (
    <View>
      <Text>{value}</Text>
    </View>
  );
};

export default GoalsList;
